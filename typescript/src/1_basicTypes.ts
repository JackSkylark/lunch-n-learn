// primitives -- undefined, null, bool, string, number
const isBool: boolean = true;

// numbers
// NOTE: all numbers in javascript are floating point
const decimal: number = 6;
const hex: number = 0xf00d;
const binary: number = 0b1010;
const octal: number = 0o744;

// strings
const basicString: string = "World";
const templatedString: string = `Hello ${basicString}!`;

// array
const list: number[] = [1, 2, 3];
const listArr: Array<number> = [1, 2, 3];

// tuple
const x: [string, number] = ["Test", 5];
